import {useState} from "react"; import {useNavigate} from "react-router-dom"; import {login} from "../api";
export default function Login(){const nav=useNavigate();const [email,setEmail]=useState("admin@keystone.local");const [password,setPassword]=useState("Password@123");const [error,setError]=useState("");const [loading,setLoading]=useState(false);
async function submit(e){e.preventDefault();setLoading(true);setError("");try{await login(email,password);nav("/")}catch(x){setError(x.response?.data?.message||"Login failed")}finally{setLoading(false)}}
return <div className="login"><div className="loginbox"><div className="logo">K</div><small>FIELD SERVICE PLATFORM</small><h2>Welcome to KEYSTONE</h2><p>Manage work orders, technicians, customers and service operations.</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button disabled={loading}>{loading?"Signing in...":"Sign in"}</button></form><div className="demo"><b>Demo accounts</b><span>admin@keystone.local</span><span>dispatcher@keystone.local</span><span>technician@keystone.local</span><span>customer@keystone.local</span><small>Password: Password@123</small></div></div></div>}