import {useState} from "react"; import {Navigate,NavLink,Route,Routes,useNavigate} from "react-router-dom";
import {currentUser,logout} from "./api"; import Login from "./pages/Login"; import Dashboard from "./pages/Dashboard"; import WorkOrders from "./pages/WorkOrders"; import WorkOrderDetails from "./pages/WorkOrderDetails"; import Customers from "./pages/Customers"; import Sites from "./pages/Sites";
function Protected({children}){return localStorage.getItem("keystone_token")?children:<Navigate to="/login" replace/>}
function Layout({children}){const nav=useNavigate();const [u]=useState(currentUser());const role=u?.role||"";return <div className="shell"><aside className="side"><div className="brand"><b>K</b><div><strong>KEYSTONE</strong><small>Field Service</small></div></div><nav><NavLink to="/" end>Dashboard</NavLink><NavLink to="/work-orders">Work Orders</NavLink>{["ADMIN","DISPATCHER"].includes(role)&&<><NavLink to="/customers">Customers</NavLink><NavLink to="/sites">Sites</NavLink></>}</nav><div className="bottom"><div className="user"><span>{(u?.name||"U")[0]}</span><div><strong>{u?.name}</strong><small>{role}</small></div></div><button onClick={()=>{logout();nav("/login")}}>Sign out</button></div></aside><main><header><div><small>FIELD SERVICE MANAGEMENT</small><h1>KEYSTONE</h1></div><span>{u?.email} <i>{role}</i></span></header><section>{children}</section></main></div>}
export default function App(){return <Routes><Route path="/login" element={<Login/>}/><Route path="/" element={<Protected><Layout><Dashboard/></Layout></Protected>}/><Route path="/work-orders" element={<Protected><Layout><WorkOrders/></Layout></Protected>}/><Route path="/work-orders/:id" element={<Protected><Layout><WorkOrderDetails/></Layout></Protected>}/><Route path="/customers" element={<Protected><Layout><Customers/></Layout></Protected>}/><Route path="/sites" element={<Protected><Layout><Sites/></Layout></Protected>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}